/**
 * @momo/consolidation
 *
 * Consolidation and deduplication for memory objects.
 * Handles semantic_hash-based deduplication and entity merging.
 */

export { Consolidator } from './consolidator';
export type { ConsolidationConfig, ConsolidationResult, ConsolidationStats } from './consolidator';
