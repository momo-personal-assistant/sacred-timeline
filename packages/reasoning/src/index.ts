/**
 * @momo/reasoning
 *
 * Reasoning layer for query responses.
 * MVP: Template-based response formatting.
 * Future: LLM-based answer generation with citations.
 */

export { SimpleReasoner } from './simple-reasoner';
export type { ReasoningConfig, ReasoningResult, SourceReference, RetrievalContext } from './types';
