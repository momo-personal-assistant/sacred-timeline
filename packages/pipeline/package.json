{
  "name": "@momo/pipeline",
  "version": "0.1.0",
  "description": "Pipeline orchestrator for memory processing stages",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@momo/chunking": "workspace:*",
    "@momo/embedding": "workspace:*",
    "@momo/graph": "workspace:*",
    "@unified-memory/db": "workspace:*",
    "@unified-memory/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
