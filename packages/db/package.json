{
  "name": "@unified-memory/db",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "pgvector": "^0.1.8",
    "@unified-memory/shared": "workspace:*",
    "@types/pg": "^8.10.9",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
